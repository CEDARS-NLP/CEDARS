"""v1-style linear workflow: query -> NLP -> adjudication -> results/download.

This package ports the original CEDARS (cedars/app) workflow onto the v2 SQL
stack so it can run as the default project workflow alongside the optional
evaluate/annotate/results flow.
"""
