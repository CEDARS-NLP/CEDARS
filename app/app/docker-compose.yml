version: '3.8'
services:
  web:
    build: .
    ports:
      - 5001:5000
    volumes:
      - .:app/
    depends_on:
      - db
  
  db:
    image: mongo:4.4.6
    ports:
      - 27018:27017
